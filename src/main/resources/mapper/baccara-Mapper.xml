<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.corelabs.baccara.mapper.BaccaraMapper">
    <insert id="insertBaccara" parameterType="baccaraVO">
        /* BaccaraMapper.insertBaccara */
        INSERT INTO tb_baccara (UID, WIN, GAME_NUM, PAIR, PAIR_B, PAIR_P, CREATE_DT)
        VALUES (
            UUID(),
            #{win},
            #{gameNum},
            #{pair},
            #{pairB},
            #{pairP},
            SYSDATE()
        )
    </insert>

</mapper>
